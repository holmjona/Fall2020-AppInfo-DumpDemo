@model List<DumpDemo.Models.Reference>

<h1>Index for Reference</h1>

@Html.ActionLink("Create New", "Create")

@foreach (DumpDemo.Models.Reference reffer in Model) {
    <p>The Item is : @reffer.Title @Html.ActionLink("Show Details", "Details", new { id = reffer.ID })</p>
}
